<UserControl x:Class="Redpack.Wpf.Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Redpack.Wpf"
             mc:Ignorable="d" 
             Background="White"
              Loaded="UserControl_Loaded">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" x:Name="ImgNum" Source="/Assets/9.png"  HorizontalAlignment="Center"/>
        <Viewbox Grid.Row="1" Stretch="Fill" Margin="5"> 
            <Label  Foreground="Red" Content="0%" x:Name="LabTitle"  HorizontalAlignment="Center"/>
        </Viewbox>

    </Grid>
</UserControl>
